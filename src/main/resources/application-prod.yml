spring:
  datasource:
    url: jdbc:mysql://mysql:3306/mojaryba
    username: root
    password: xxx

  liquibase:
    contexts: prod

  web:
    resources:
      static-locations: classpath:/static/,file:uploads/

app:
  storage:
    location: uploads/photos/
  email:
    url: http://moja-ryba.pl
